你是资深 AI Agent 架构师和项目管理专家。请为“面向研发的 AI Coding Agent 治理轨道”项目制定一份可执行的实施计划。不要直接写代码或创建文件，先只输出计划，等待我确认。

## 项目背景
我们要构建一个面向研发的 coding agent 工具。它不是替代 Claude Code、Cursor、OpenHands 等代码生成引擎，而是包裹在它们外面的“治理层”和“有轨列车”。它参考软考系统集成项目管理工程师的五大过程组、十大知识域，把项目管理思想变成 Agent 运行时约束。目标：
- AI 做研发像有轨列车，稳定、可观测、能自动发现问题并纠正。
- 自动生成各知识域、各子过程的轻量项目文件。
- 每个项目文件都必须链接真实事件证据，防止 LLM 幻觉。
- 关键决策点有质量门，不通过就阻塞或触发纠正措施。

## 核心原则
1. 过程即代码：过程组/知识域裁剪为 YAML/JSON 工作流，不是纸质文档。
2. 工件即证据：项目文件必须链接 commit、PR、测试报告、决策记录、issue 等。
3. 门禁即信号：阶段质量门不通过就阻塞，Agent 不能绕过。
4. 变更即分支：范围/架构/进度/成本基线变更必须走 CR + 影响分析 + 审批。
5. 度量即仪表：用 SPI/CPI、缺陷逃逸率、周期时间、返工率驱动纠偏。
6. 确定性优先：EVM、测试通过率、覆盖率、漏洞扫描等能确定性验证的，不用 LLM 判断。
7. 人类在环：范围基线、架构决策、发布、重大变更必须人类审批。
8. 轻量裁剪：研发项目可裁剪采购、干系人等，避免文档垃圾。
9. 安全隔离：所有生成代码必须在沙箱运行，权限分级。
10. 可观测：OpenTelemetry + 事件日志形成黑匣子，任何偏差可回溯。

## 必须覆盖的治理域
整合、范围、进度、成本、质量、资源、沟通、风险、采购、干系人。
重点：整合、范围、质量、风险、进度、成本、变更。

## 推荐架构
- 编排层：Temporal / LangGraph，承载“启动→规划→执行→监控→收尾”循环。
- 角色层：PM Agent、Architect Agent、Dev Agent、QA Agent、Risk Agent、Scribe Agent。通过结构化 ProjectState 协作，不靠自由文本对话。
- 治理层：OPA/Rego 或策略引擎，在 Intent Guard、Tool Guide、Human-in-the-Loop Gate 注入策略。
- 执行沙箱：隔离运行代码。
- 可观测层：OpenTelemetry + 事件日志。
- 模型分层：强模型编码，中等模型结构化，确定性代码做门禁/EVM，轻量模型做报告。
- 集成形态：MVP 为 CLI + Git；扩展为 IDE 插件 / MCP Server。
- 底层复用现有 Coding Agent，不自研代码生成引擎。

## 实施路线参考
Phase 1：最小治理闭环（范围门、质量门、变更门；LangGraph + 文件系统 + OPA + 现有 Agent CLI）。
Phase 2：EVM 与多 Agent 协作（SPI/CPI 自动计算、PM/QA 角色分离、生成-评审-修复闭环）。
Phase 3：持久化与生产化（Temporal、跨项目知识库、自动根因分析、经验教训沉淀）。

## 你的任务
1. 先只读探索当前仓库：查看 README、现有代码结构、依赖文件、文档、CI 配置。如果仓库为空，明确说明并基于假设制定。
2. 输出一份实施计划，必须包含：
   - 目标与非目标
   - 用户画像与核心场景
   - 系统上下文与架构图（文字或 Mermaid）
   - 核心数据模型：ProjectState、工件、门禁、变更请求、风险登记册、EVM 快照
   - 状态机：过程组循环与阶段门
   - 目录结构与模块划分
   - 技术选型与理由
   - 门禁策略示例（YAML/Rego 风格）
   - 自动生成的工件清单与模板（轻量、证据链接）
   - 知识域到 Agent 行为、工件、检查点的映射表
   - 与现有 Coding Agent 的集成方式
   - 分阶段任务分解：每项 0.5-2 天，标注依赖、负责角色、验收标准
   - MVP 范围与成功指标（含 MTTR、变更失败率、返工率、计划偏差、逃逸缺陷）
   - 风险与开放问题，需要我决策的事项
3. 计划要可执行、可验证、避免过度流程。用中文输出 Markdown。先不要创建或修改任何文件。
4. 如果信息不足，先列出你的假设，再给出计划；不要直接开始实现。